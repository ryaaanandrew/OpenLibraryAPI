/// <reference types="cypress" />

describe("Plan admin company plan view", () => {
  beforeEach(() => {
    cy.visit("http://localhost:3000/");
  });

  it("Plan Admin Dashboard should render with all stats", () => {});
});
